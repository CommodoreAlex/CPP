
```cpp
#include <iostream>

// The old c/C++ way of creating a type alias - a new name for an existing type
typedef int my_int;

// This is exactly what using does
using my_int = int;

// Both of these create a type alias but using is the modern, cleaner version.
```
